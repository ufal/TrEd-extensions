# -*- cperl -*-

#ifndef pdt_vallex
#define pdt_vallex

{

package ValLex::GUI; # so that it gets deleted
use lib TredMacro::CallerDir();
BEGIN {
  require ValLex::GUI;
}

}

#include "non_gui.mak"

#include "pml_vallex.mak"

#endif pdt_vallex
